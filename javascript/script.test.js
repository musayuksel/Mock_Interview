const { sumNumbers, getNames } = require("./script");

describe("sumNumbers function:", () => {
  test("should return sum when two arguments are provided", () => {
    // Assert sum of two numbers
  });

  test("should return argument itself when one arguments are provided", () => {
    // Assert return argument itself if there is one argument
  });

  test("should return 0 when no arguments are provided", () => {
    // Assert return 0 when no argument
  });

  test("should return sum when more arguments are provided", () => {
    // Assert sum of three numbers
  });
});

describe("getNames function:", () => {
  test("should return empty array when the argument is empty array", () => {
    // Assert
  });

  test("should return array of names when the argument array objects has name", () => {
    // Assert
  });

  test("should return empty array when the argument array objects has no name", () => {
    // Assert
  });

  test("should return only array of names when the some argument array objects has not name ", () => {
    // Assert
  });
});
